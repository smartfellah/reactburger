const initialState = [
  {
    currentIngredient: {},
  },
];

export const singleIngredientReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
