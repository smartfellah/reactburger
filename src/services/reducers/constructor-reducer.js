const initialState = {
  bun: {},
  data: [],
  totalCost: 0,
};

export const constructorReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
